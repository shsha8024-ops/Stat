# الغدير نقل و تخليص — Full Stack Application

نظام إدارة العملاء والفواتير متعدد الفواتير لكل عميل مع نظام تسجيل دخول وصلاحيات.

## 🎯 المميزات

### 🔐 نظام المستخدمين والصلاحيات
- ✅ تسجيل دخول آمن
- ✅ نظام صلاحيات (مدير / موظف)
- ✅ **المدير:** صلاحيات كاملة (إضافة، تعديل، حذف)
- ✅ **الموظف:** عرض فقط (الفواتير والقبوضات والحساب النهائي)

**حسابات المستخدمين:**
- مدير: `star` / `star1996@`
- موظف: `muhanad` / `muhanad1996`
- موظف: `bedrxan` / `badrxan1993`

### 🎨 نظام الثيمات
- ✅ 4 ثيمات احترافية مع زر دائري
- 🟣 **الافتراضي:** أزرق بنفسجي
- 🌊 **المحيط:** أزرق سماوي
- 🌅 **الغروب:** برتقالي وردي
- 🌲 **الغابة:** أخضر زمردي

### إدارة العملاء
- ✅ إضافة، تعديل، حذف العملاء (مدير فقط)
- ✅ البحث بالاسم، الهاتف، المكان
- ✅ اختيار العملة لكل عميل ($, IQD, USD)

### إدارة الفواتير
- ✅ فواتير متعددة لكل عميل
- ✅ جداول ديناميكية قابلة للتعديل (مدير فقط):
  - جدول العمليات: رقم | التاريخ | اسم السائق | رقم السيارة | ملاحظة | المبلغ
  - جدول القبوضات: رقم | التاريخ | اسم السائق | رقم السيارة | ملاحظة | المبلغ
- ✅ إضافة/حذف أسطر وأعمدة ديناميكياً (مدير فقط)
- ✅ حماية عمود المبلغ من الحذف
- ✅ حساب تلقائي للأرصدة
- ✅ الموظف: عرض فقط (قراءة)

### التقارير
- ✅ ملخص أرصدة جميع العملاء
- ✅ تصدير Excel
- ✅ عرض عدد الفواتير لكل عميل

### النسخ الاحتياطي
- ✅ تصدير واستيراد البيانات بصيغة JSON (مدير فقط)

## 🏗️ التقنيات المستخدمة

### Backend
- **FastAPI** - Python web framework
- **MongoDB** - قاعدة البيانات
- **Motor** - MongoDB async driver
- **Pydantic** - Data validation

### Frontend
- **React 18** - UI library
- **React Router** - للتنقل بين الصفحات
- **Context API** - لإدارة Authentication و Themes
- **CSS Variables** - نظام ثيمات ديناميكي
- **Axios** - للاتصال بالـ API
- **SheetJS (xlsx)** - تصدير Excel

## 📁 هيكل المشروع

```
/app/
├── backend/
│   ├── server.py              # FastAPI مع Authentication
│   ├── requirements.txt
│   └── .env
│
├── frontend/
│   ├── src/
│   │   ├── contexts/
│   │   │   ├── AuthContext.js       # إدارة المستخدمين
│   │   │   └── ThemeContext.js      # إدارة الثيمات
│   │   ├── pages/
│   │   │   ├── LoginPage.js         # صفحة تسجيل الدخول
│   │   │   ├── ClientsPage.js       # صفحة العملاء
│   │   │   ├── InvoicePage.js       # صفحة الفواتير
│   │   │   └── ReportsPage.js       # صفحة التقارير
│   │   ├── components/
│   │   │   ├── TableEditor.js       # محرر الجداول
│   │   │   └── ThemeSwitcher.js     # مبدّل الثيمات
│   │   └── App.js
│   ├── package.json
│   └── .env
│
└── old_static_version/          # النسخة القديمة Static
```

## 🚀 التشغيل

### تشغيل كل شيء (باستخدام Supervisor)
```bash
sudo supervisorctl restart all
sudo supervisorctl status
```

## 🔗 API Endpoints

### Authentication
- `POST /api/auth/login` - تسجيل الدخول

### العملاء
- `GET /api/clients` - جلب جميع العملاء
- `POST /api/clients` - إضافة عميل جديد (مدير فقط)
- `GET /api/clients/{id}` - جلب عميل محدد
- `PUT /api/clients/{id}` - تحديث عميل (مدير فقط)
- `DELETE /api/clients/{id}` - حذف عميل (مدير فقط)

### الفواتير
- `GET /api/clients/{id}/invoices` - جلب فواتير العميل
- `POST /api/clients/{id}/invoices` - إنشاء فاتورة (مدير فقط)
- `GET /api/invoices/{id}` - جلب فاتورة محددة
- `PUT /api/invoices/{id}` - تحديث فاتورة (مدير فقط)
- `DELETE /api/invoices/{id}` - حذف فاتورة (مدير فقط)

### البيانات
- `GET /api/invoices/{id}/statement` - جلب بيانات الفاتورة
- `PUT /api/invoices/{id}/statement` - تحديث بيانات الفاتورة (مدير فقط)

### التقارير
- `GET /api/reports` - جلب تقارير جميع العملاء

### النسخ الاحتياطي
- `GET /api/backup` - تصدير نسخة احتياطية (مدير فقط)
- `POST /api/restore` - استعادة نسخة احتياطية (مدير فقط)

## 🎨 الثيمات

التطبيق يدعم 4 ثيمات قابلة للتبديل:

1. **الافتراضي 🟣** - أزرق بنفسجي كلاسيكي
2. **المحيط 🌊** - أزرق سماوي منعش
3. **الغروب 🌅** - برتقالي وردي دافئ
4. **الغابة 🌲** - أخضر زمردي هادئ

**كيفية التبديل:** اضغط على الزر الدائري 🎨 في أسفل اليسار!

## 🔐 الصلاحيات

### المدير (star):
- ✅ إضافة وتعديل وحذف العملاء
- ✅ إنشاء وتعديل وحذف الفواتير
- ✅ تعديل الجداول (العمليات والقبوضات)
- ✅ النسخ الاحتياطي والاستعادة
- ✅ عرض التقارير
- ✅ تصدير Excel و PDF

### الموظف (muhanad, bedrxan):
- ✅ عرض قائمة العملاء (بدون إضافة/تعديل)
- ✅ عرض الفواتير (للقراءة فقط)
- ✅ عرض الجداول (بدون تعديل)
- ✅ عرض الحساب النهائي
- ✅ عرض التقارير
- ✅ تصدير Excel و PDF

## 📝 ملاحظات

- جميع البيانات محفوظة في MongoDB
- حفظ تلقائي للتعديلات
- واجهة عربية RTL كاملة
- تصميم responsive يعمل على جميع الأجهزة
- دعم الطباعة والتصدير
- نظام أمان بسيط (يمكن تطويره)

## 🔐 Environment Variables

### Backend (.env)
```
MONGO_URL=mongodb://localhost:27017
MONGO_DB_NAME=alghadeer_db
```

### Frontend (.env)
```
REACT_APP_BACKEND_URL=http://localhost:8001
```

## 📱 الصفحات

1. **صفحة تسجيل الدخول** (`/login`) - تسجيل دخول آمن
2. **صفحة العملاء** (`/`) - إدارة العملاء (مدير) أو عرض (موظف)
3. **صفحة الفواتير** (`/invoice/:clientId`) - إدارة الفواتير حسب الصلاحية
4. **صفحة التقارير** (`/reports`) - ملخص الأرصدة

## 🎨 التصميم

- استخدام CSS Variables للثيمات الديناميكية
- 4 ثيمات احترافية قابلة للتبديل بزر واحد
- واجهة عربية RTL
- تصميم حديث وسهل الاستخدام
- دعم الطباعة
- دعم التصدير لـ Excel

---

تم التطوير بواسطة **E1 Agent** على منصة **Emergent** 🚀
