# الغدير نقل و تخليص — Full Stack Application

نظام إدارة العملاء والفواتير متعدد الفواتير لكل عميل.

## 🎯 المميزات

### إدارة العملاء
- ✅ إضافة، تعديل، حذف العملاء
- ✅ البحث بالاسم، الهاتف، المكان
- ✅ اختيار العملة لكل عميل ($, IQD, USD)

### إدارة الفواتير
- ✅ فواتير متعددة لكل عميل
- ✅ جداول ديناميكية قابلة للتعديل:
  - جدول العمليات: رقم | التاريخ | اسم السائق | رقم السيارة | ملاحظة | المبلغ
  - جدول القبوضات: رقم | التاريخ | اسم السائق | رقم السيارة | ملاحظة | المبلغ
- ✅ إضافة/حذف أسطر وأعمدة ديناميكياً
- ✅ حماية عمود المبلغ من الحذف
- ✅ حساب تلقائي للأرصدة

### التقارير
- ✅ ملخص أرصدة جميع العملاء
- ✅ تصدير Excel
- ✅ عرض عدد الفواتير لكل عميل

### النسخ الاحتياطي
- ✅ تصدير واستيراد البيانات بصيغة JSON

## 🏗️ التقنيات المستخدمة

### Backend
- **FastAPI** - Python web framework
- **MongoDB** - قاعدة البيانات
- **Motor** - MongoDB async driver
- **Pydantic** - Data validation

### Frontend
- **React 18** - UI library
- **React Router** - للتنقل بين الصفحات
- **Tailwind CSS** - التصميم
- **Axios** - للاتصال بالـ API
- **SheetJS (xlsx)** - تصدير Excel
- **jsPDF** - تصدير PDF

## 📁 هيكل المشروع

```
/app/
├── backend/              # FastAPI Backend
│   ├── server.py        # Main API
│   ├── requirements.txt # Python dependencies
│   └── .env            # Environment variables
│
├── frontend/            # React Frontend
│   ├── src/
│   │   ├── App.js
│   │   ├── pages/
│   │   │   ├── ClientsPage.js     # صفحة العملاء
│   │   │   ├── InvoicePage.js     # صفحة الفواتير
│   │   │   └── ReportsPage.js     # صفحة التقارير
│   │   └── components/
│   │       └── TableEditor.js     # محرر الجداول الديناميكي
│   ├── package.json
│   └── .env
│
└── README.md
```

## 🚀 التشغيل

### المتطلبات
- Python 3.8+
- Node.js 16+
- MongoDB

### تشغيل Backend
```bash
cd /app/backend
pip install -r requirements.txt
python server.py
```

Backend يعمل على: http://localhost:8001

### تشغيل Frontend
```bash
cd /app/frontend
yarn install
yarn start
```

Frontend يعمل على: http://localhost:3000

### تشغيل كل شيء (باستخدام Supervisor)
```bash
sudo supervisorctl restart all
sudo supervisorctl status
```

## 🔗 API Endpoints

### العملاء
- `GET /api/clients` - جلب جميع العملاء
- `POST /api/clients` - إضافة عميل جديد
- `GET /api/clients/{id}` - جلب عميل محدد
- `PUT /api/clients/{id}` - تحديث عميل
- `DELETE /api/clients/{id}` - حذف عميل

### الفواتير
- `GET /api/clients/{id}/invoices` - جلب فواتير العميل
- `POST /api/clients/{id}/invoices` - إنشاء فاتورة جديدة
- `GET /api/invoices/{id}` - جلب فاتورة محددة
- `PUT /api/invoices/{id}` - تحديث فاتورة
- `DELETE /api/invoices/{id}` - حذف فاتورة

### البيانات
- `GET /api/invoices/{id}/statement` - جلب بيانات الفاتورة
- `PUT /api/invoices/{id}/statement` - تحديث بيانات الفاتورة

### التقارير
- `GET /api/reports` - جلب تقارير جميع العملاء

### النسخ الاحتياطي
- `GET /api/backup` - تصدير نسخة احتياطية
- `POST /api/restore` - استعادة نسخة احتياطية

## 📝 ملاحظات

- جميع البيانات محفوظة في MongoDB
- حفظ تلقائي للتعديلات
- واجهة عربية RTL
- تصميم responsive يعمل على جميع الأجهزة
- قابل للنشر على Emergent

## 🔐 Environment Variables

### Backend (.env)
```
MONGO_URL=mongodb://localhost:27017
```

### Frontend (.env)
```
REACT_APP_BACKEND_URL=http://localhost:8001
```

## 📱 الصفحات

1. **صفحة العملاء** (`/`) - إدارة العملاء
2. **صفحة الفواتير** (`/invoice/:clientId`) - إدارة فواتير العميل
3. **صفحة التقارير** (`/reports`) - ملخص الأرصدة

## 🎨 التصميم

- استخدام Tailwind CSS للتصميم
- واجهة عربية RTL
- تصميم حديث وسهل الاستخدام
- دعم الطباعة
- دعم التصدير لـ Excel

## 📄 الترخيص

هذا المشروع مملوك لشركة الغدير نقل و تخليص.

---

تم التطوير بواسطة **E1 Agent** على منصة **Emergent** 🚀
